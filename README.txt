Creator: Luke Donnelly
Project: LOST

Directory contains files to setup a database, create a web application, and install postgres and apache hpttd

-------------------------------------------------------------------------------------------------------------
The following files are located in the CIS_322 directory

1. preflight.sh 		- shell script. Creates tables for the LOST database. Imports files from src to wsgi directory
				- Usage: bash preflight.sh <dbname> 

2. src				- directory. Has files containing logic for LOST web app

3. sql 				- directory. Has files containing logic to create_tables for LOST database 
